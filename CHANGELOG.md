# Change Log

## [Unreleased]

- Insert a header comment with the file's workspace-relative path when a new file is created.
- Configurable header fields (path, creation date, author, description).
- Configurable comment styles per file extension, merged over built-in defaults.
- `Insert File Header` command for existing files.
